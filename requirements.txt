# Direct dependencies only. Everything else (ctranslate2, faster-whisper,
# pyannote-audio, torchcodec, transformers, ...) arrives transitively via
# whisperx -- pinning them here would fight whisperx's own resolver on upgrade.
#
# torch/torchaudio are pinned even though whisperx requires them: the MPS
# diarization path in scripts/mps_pipeline.py is sensitive to the torch
# version, and an unpinned resolve has silently produced builds where
# device="mps" falls back to CPU for every op.
whisperx==3.8.6
torch==2.8.0
torchaudio==2.8.0
